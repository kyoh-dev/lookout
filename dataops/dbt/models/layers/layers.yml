version: 2

models:
  - name: park
    columns:
      - name: id
        tests:
          - not_null
          - unique
      - name: iucn_code
        tests:
          - capitalised
      - name: geometry
        tests:
          - not_null
          - is_multipolygon
